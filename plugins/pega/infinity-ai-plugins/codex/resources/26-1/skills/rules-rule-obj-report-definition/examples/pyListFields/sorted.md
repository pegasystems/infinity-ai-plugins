---
name: List Field — Sorted column
description: "A single pyListFields entry — sorted column. pySortOrder is the sort-priority rank (1 = primary, 2 = secondary, etc.) and MUST be paired with pySortType. For unsorted columns, omit both pySortOrder and pySortType entirely."
---

```json
{
  "pyFieldName": ".pyID",
  "pyFieldLabel": "Case ID",
  "pySortOrder": "1",
  "pySortType": "ASC"
}
```
