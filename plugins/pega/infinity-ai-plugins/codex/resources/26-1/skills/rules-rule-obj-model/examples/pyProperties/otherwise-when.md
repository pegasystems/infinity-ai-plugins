---
name: Data Transform pyProperties OTHERWISE_WHEN
description: Else-if branch in a WHEN/OTHERWISE group. Evaluated only when all preceding WHEN and OTHERWISE_WHEN conditions were false.
---

```json
{
  "pyActionName": "OTHERWISE_WHEN",
  "pyPropertiesName": ".pyUrgencyWorkClass>=50",
  "pyExpanded": "true",
  "pyProperties": [
    {
      "pyActionName": "SET",
      "pyPropertiesName": ".PriorityLabel",
      "pyPropertiesValue": "\"High\""
    }
  ]
}
```
